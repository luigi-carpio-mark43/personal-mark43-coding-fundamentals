-- Exercise 1: Reports by department
--
-- Goal: Return all active reports belonging to department 42,
--       ordered by created_date_utc descending (newest first).
--
-- Select these columns (in this order):
--   id, report_number, report_title, approval_status, created_date_utc
--
-- Filters:
--   - department_id = 42
--   - is_deleted = false   (exclude soft-deleted reports)
--
-- Tip: In Mark43, "active" almost always means is_deleted = false.

-- Write your query below (replace this placeholder):
SELECT 1 WHERE false;
